#!/bin/sh

TRANTOR_DEBUG_OPTS="-Xdebug -Xrunjdwp:transport=dt_socket,server=y,suspend=y,address=9000"

echo Preparing to execute Trantor in debug mode

env TRANTOR_DEBUG_OPTS="$TRANTOR_DEBUG_OPTS" "$(dirname "$0")/trantor" "$@"
